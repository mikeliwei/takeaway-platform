<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.DishMapper">


    <insert id="insertWithFlavor" parameterType="com.sky.entity.Dish" useGeneratedKeys="true" keyProperty="id">
        insert into dish (category_id, name, image, price, description, status, create_time, update_time, create_user, update_user)
        values (#{categoryId}, #{name}, #{image}, #{price}, #{description}, #{status}, #{createTime}, #{updateTime}, #{createUser}, #{updateUser})
    </insert>
    <update id="update">
        update dish
        <set>
            <if test="name != null and name != ''">
                name = #{name},
            </if>
            <if test="price != null">
                price = #{price},
            </if>
            <if test="description != null and description != ''">
                description = #{description},
            </if>
            <if test="categoryId != null">
                category_id = #{categoryId},
            </if>
            <if test="image != null">
                image = #{image},
            </if>
            <if test="updateTime != null">
                update_time = #{updateTime},
            </if>
            <if test="updateUser != null">
                update_user = #{updateUser},
            </if>
            <if test="status != null">
                status = #{status},
            </if>
        </set>
        where id = #{id}
    </update>
    <delete id="deleteBatch">
        delete from dish where id in
        <foreach collection="ids" item="id" open="(" close=")" separator=",">
            #{id}
        </foreach>
    </delete>
    <select id="page" resultType="com.sky.vo.DishVO" >
        select d.*, c.name as categoryName from dish d left join category c on d.category_id = c.id
        <where>
            <if test="status != null">
                and d.status = #{status}
            </if>
            <if test="name != null and name.trim() != ''">
                and d.name like concat('%', #{name}, '%')
            </if>
            <if test="categoryId != null">
                and d.category_id = #{categoryId}
            </if>
        </where>
        order by d.create_time desc
    </select>
    <resultMap id="dishVo" type="com.sky.vo.DishVO" autoMapping="true">
        <id column="id" property="id" />
        <collection property="flavors" ofType="com.sky.entity.DishFlavor" autoMapping="true" >
            <id column="flavorId" property="id" />
            <result column="flavorName" property="name" />
            <result column="dish_id" property="dishId" />
            <result column="value" property="value" />
        </collection>
    </resultMap>
    <select id="select" resultMap="dishVo">
        select
            d.id,
            d.category_id,
            d.name,
            d.image,
            d.price,
            d.description,
            d.status,
            d.create_time,
            d.update_time,
            df.id as flavorId,
            df.name as flavorName,
            df.dish_id,
            df.value
            from dish d left join dish_flavor df on d.id = df.dish_id
        where d.id = #{id}
    </select>
</mapper>
